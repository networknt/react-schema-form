To rebuild the example form.entry.js, use the following command. 

```
webpack -p
```

It will generate a dist directory with three files. Copy these files to the parent example folder and remove the dist folder. 

